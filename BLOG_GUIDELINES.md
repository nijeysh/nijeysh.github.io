# 블로그 작성 가이드라인 (Blog Writing Guidelines)

본 문서는 일관되고 품질 높은 기술 블로그 포스트를 지속적으로 작성하기 위한 기본 원칙과 규칙을 정의합니다.

---

## 1. 말투 및 톤 (Tone & Voice)

- **기본 말투**: `~이다`, `~했다` 등 평어체를 사용한다.
- **성격**: 단순 이론 나열보다는 본인의 경험담과 그를 통해 알게 된 지식을 공유하는 친근하지만 명확한 톤을 지향한다.

## 2. 글의 기본 구조 (Content Structure)

포스트는 가급적 아래의 기본적인 흐름을 따른다.

1. **서론 (도입부)**
   - 글을 작성하게 된 계기, 직면했던 문제 상황(에러, 시행착오 등), 혹은 구현하고자 했던 목표 등을 명시한다.
2. **본론**
   - 핵심적인 개념 설명, 명령어, 코드 예제 등을 포함한다. 
   - 해결 방식이 어떻게 이루어졌는지 논리적으로 작성한다.
3. **요약 / 결론**
   - 이 글을 통해 새롭게 알게 된 점, 주의할 점, 배운 점 등을 3줄 내외로 간략하게 요약한다.
4. **참고자료 (Reference)**
   - 관련된 공식 문서 링크나 도움을 받은 타 블로그의 출처를 반드시 명시한다.

## 3. 메타데이터 작성 규칙 (Front-matter)

게시물의 상단(Front-matter)은 반드시 아래의 양식에 맞추어 작성한다.

```yaml
---
layout: post
title: "포스트의 제목을 명확히 작성"
category: "카테고리명"
tags: [tag1, tag2]
---
```

- **`layout`**: 무조건 `post` 로 고정한다.
- **`title`**: 글의 제목을 작성한다.
- **`category`**: 소문자 형식으로 작성하며, `_config.yml`의 `tech_submenu` 혹은 `algorithm`, `git` 등에 사전 정의된 카테고리만 사용한다. (예: `java`, `spring`, `ai`, `git`, `web`)
- **`tags`**: 소문자 영문으로 작성하여 통일감을 유지한다. 배열 형태인 `[tag1, tag2]` 로 작성한다.

## 4. 제목 및 헤딩 규칙 (Headings)

- **`#` (h1)**: 글의 큰 제목에 해당하며 프론트매터의 `title` 이 이를 대체하므로 가급적 본문 내에서는 생략한다.
- **`##` (h2)**, **`###` (h3)**: 본문의 소제목과 섹션을 분리할 때 사용한다.
- **제목 스타일**: 명사형 종결(`~하는 방법`, `~에 대한 이해`)이나 질문형(`~란 무엇인가?`)은 문맥과 때에 따라 자연스럽게 유동적으로 선택한다.

## 5. AI Assistant 가이드 (카테고리 및 태그 작성 규칙)

**사이드바 메뉴가 포스트 분류의 절대적 기준입니다.**

AI가 블로그 포스트를 셋업하거나 렌더링 설정을 할 때 다음 원칙을 따릅니다:
1. **Category (대분류)**: 포스트의 `category`는 무조건 `_config.yml`에 존재하는 사이드바 메뉴(`tech_submenu`의 `category` 속성)와 1:1로 일치해야 합니다. (예: 도커나 쿠버네티스 글이더라도 기준은 사이드바 메뉴이므로 `category: devops`로 고정합니다.)
2. **Tags (소분류)**: 구체적인 세부 기술 스택(예: Docker, Kubernetes, OneDev, CI/CD 등)은 `category` 대신 무조건 `tags`에 배열 형태로 기입하여 검색 및 필터링에 사용합니다. (예: `tags: [docker, kubernetes]`)
3. **새 사이드바 메뉴 추가 시**: HTML 파일은 절대로 직접 수정하지 않으며, 오직 `_config.yml`의 `tech_submenu` 블록에 객체를 추가하여 사이드바가 동적으로 렌더링되게 합니다.
위 규칙을 통해 사이드바가 난잡해지지 않도록 통제하고, 세부 분류는 태그에 온전히 위임합니다.

---

## 6. Git 작업 규칙 (Commit & Push)

AI Assistant가 변경 사항을 커밋하거나 푸시할 때는 아래 순서를 반드시 따른다.

1. 변경 내용을 확인한 뒤, 먼저 커밋 메시지를 추천한다.
2. 사용자가 추천된 커밋 메시지를 승인하거나 다른 메시지를 지정하기 전까지는 커밋하지 않는다.
3. 사용자가 명시적으로 푸시를 요청하거나 승인하기 전까지는 푸시하지 않는다.
4. 커밋 메시지는 사용자의 요청이 있으면 한국어로 작성한다.
5. 숨김 글이나 초안 글은 `published: false` 여부를 커밋 전에 확인한다.
