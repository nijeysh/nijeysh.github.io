# 블로그 작성 가이드라인 (Blog Writing Guidelines)

본 문서는 일관되고 품질 높은 기술 블로그 포스트를 지속적으로 작성하기 위한 기본 원칙과 규칙을 정의합니다.

---

## 1. 말투 및 톤 (Tone & Voice)

- **기본 말투**: `~이다`, `~했다` 등 평어체를 사용한다.
- **성격**: 단순 이론 나열보다는 본인의 경험담과 그를 통해 알게 된 지식을 공유하는 친근하지만 명확한 톤을 지향한다.

## 2. 글의 기본 구조 (Content Structure)

포스트는 가급적 아래의 기본적인 흐름을 따른다.

1. **서론 (도입부)**
   - 글을 작성하게 된 계기, 직면했던 문제 상황(에러, 시행착오 등), 혹은 구현하고자 했던 목표 등을 명시한다.
2. **본론**
   - 핵심적인 개념 설명, 명령어, 코드 예제 등을 포함한다. 
   - 해결 방식이 어떻게 이루어졌는지 논리적으로 작성한다.
3. **요약 / 결론**
   - 이 글을 통해 새롭게 알게 된 점, 주의할 점, 배운 점 등을 3줄 내외로 간략하게 요약한다.
4. **참고자료 (Reference)**
   - 관련된 공식 문서 링크나 도움을 받은 타 블로그의 출처를 반드시 명시한다.

## 3. 메타데이터 작성 규칙 (Front-matter)

게시물의 상단(Front-matter)은 반드시 아래의 양식에 맞추어 작성한다.

```yaml
---
layout: post
title: "포스트의 제목을 명확히 작성"
category: "카테고리명"
tags: [tag1, tag2]
---
```

- **`layout`**: 무조건 `post` 로 고정한다.
- **`title`**: 글의 제목을 작성한다.
- **`category`**: 소문자 형식으로 작성하며, `_config.yml`의 `tech_submenu` 혹은 `algorithm`, `git` 등에 사전 정의된 카테고리만 사용한다. (예: `java`, `spring`, `git`, `web`)
- **`tags`**: 소문자 영문으로 작성하여 통일감을 유지한다. 배열 형태인 `[tag1, tag2]` 로 작성한다.

## 4. 제목 및 헤딩 규칙 (Headings)

- **`#` (h1)**: 글의 큰 제목에 해당하며 프론트매터의 `title` 이 이를 대체하므로 가급적 본문 내에서는 생략한다.
- **`##` (h2)**, **`###` (h3)**: 본문의 소제목과 섹션을 분리할 때 사용한다.
- **제목 스타일**: 명사형 종결(`~하는 방법`, `~에 대한 이해`)이나 질문형(`~란 무엇인가?`)은 문맥과 때에 따라 자연스럽게 유동적으로 선택한다.
